LOG_LEVEL=info
LOG_FORMAT=simple
LOG_SLOW_REQUEST_THRESHOLD=500
LOG_SKIP_PATHS=/health

# Authentication Configuration
JWT_SECRET=your-super-secret-key-minimum-32-characters-long
JWT_REFRESH_SECRET=your-super-refresh-secret-key-minimum-32-characters
JWT_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_MAX_REQUESTS=5

# Rate limiting configuration
RATE_LIMIT_TTL=60000
RATE_LIMIT_MAX=100
RATE_LIMIT_AUTH_TTL=60000
RATE_LIMIT_AUTH_MAX=5
RATE_LIMIT_STRICT_TTL=60000
RATE_LIMIT_STRICT_MAX=10

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=password
DB_NAME=chioma_db
PAYMENT_METADATA_SECRET=change_me
PAYMENT_WEBHOOK_SECRET=change-me-webhook-secret-minimum-32-characters-long

# AWS S3 Storage
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_S3_BUCKET=
PAYMENT_GATEWAY=mock
PAYSTACK_SECRET_KEY=erhrgehr
FLUTTERWAVE_SECRET_KEY=egerherhgd
PAYMENT_GATEWAY_TIMEOUT_MS=10000

# Application Configuration
NODE_ENV=development
PORT=3000

# Admin Seed Configuration
ADMIN_DEFAULT_EMAIL=admin@chioma.local
ADMIN_DEFAULT_FIRST_NAME=System
ADMIN_DEFAULT_LAST_NAME=Administrator
ADMIN_AUTO_GENERATE_PASSWORD=true

# Escrow Contract Configuration
ESCROW_CONTRACT_ID=<your-escrow-contract-id>
DEFAULT_ARBITER_ADDRESS=<default-arbiter-stellar-address>

# Dispute Resolution Contract Configuration
DISPUTE_CONTRACT_ID=<your-dispute-contract-id>
MIN_VOTES_REQUIRED=3

# Health Check Configuration
STELLAR_HORIZON_URL=https://horizon-testnet.stellar.org
HEALTH_CHECK_TIMEOUT=5000
MEMORY_WARNING_THRESHOLD=536870912
MEMORY_ERROR_THRESHOLD=1073741824

# Email Configuration (for password reset emails)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
EMAIL_FROM=noreply@chioma.app

# Frontend Configuration (for password reset / email verification links)
FRONTEND_URL=http://localhost:3000
PASSWORD_RESET_URL=http://localhost:3000/reset-password

# Stellar/Soroban Configuration
STELLAR_NETWORK=testnet
SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
CHIOMA_CONTRACT_ID=
ESCROW_CONTRACT_ID=
RENT_OBLIGATION_CONTRACT_ID=
STELLAR_ADMIN_SECRET_KEY=
DEFAULT_ARBITER_ADDRESS=
SERVER_STELLAR_SECRET=herherg
STELLAR_SERVER_SECRET_KEY=rehger

# IPFS/Pinata Configuration
PINATA_JWT=rherrge
PINATA_GATEWAY=gateway.pinata.cloud

# Anchor Configuration
ANCHOR_API_URL=https://api.anchor-provider.com
ANCHOR_API_KEY=your_api_key
ANCHOR_USDC_ASSET=USDC:GA5ZSEJYB37JRC5AVCIA5MOP4RHTM335X2KGX3IHOJAPP5RE34K4KZVN
SUPPORTED_FIAT_CURRENCIES=USD,EUR,GBP,NGN

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
# For Upstash (serverless Redis), use these instead:
# REDIS_URL=https://your-upstash-redis-url.upstash.io
# REDIS_TOKEN=your-upstash-token
REDIS_USERNAME=default
REDIS_TLS=false

# Bull Job Queue Configuration
BULL_QUEUE_EMAIL_ATTEMPTS=3
BULL_QUEUE_EMAIL_BACKOFF_DELAY=2000
BULL_QUEUE_DOCUMENTS_ATTEMPTS=3
BULL_QUEUE_DOCUMENTS_BACKOFF_DELAY=3000
BULL_QUEUE_BLOCKCHAIN_ATTEMPTS=5
BULL_QUEUE_BLOCKCHAIN_BACKOFF_DELAY=5000
BULL_QUEUE_DATA_SYNC_ATTEMPTS=3
BULL_QUEUE_DATA_SYNC_BACKOFF_DELAY=2000

# Sentry Configuration
SENTRY_DSN=
SENTRY_ENVIRONMENT=development

# Security Configuration
SECURITY_ENCRYPTION_KEY=YOUR_SECURITY_ENCRYPTION_KEY
# CSRF protection is enabled by default. Set to 'false' only for local development
# environments where a browser frontend is not involved (e.g. pure API testing).
SECURITY_CSRF_ENABLED=true
SECURITY_SESSION_SECRET=your-session-secret-minimum-32-characters-long

# Webhook Secret (required — used to verify HMAC-SHA256 signatures on inbound payment webhook calls)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PAYMENT_WEBHOOK_SECRET=
# Comma-separated IPv4 addresses/CIDR ranges allowed to reach the admin panel
# endpoints (/api/admin/*). Leave empty to leave access control to the normal
# auth/role guards. ADMIN_IP_BLACKLIST always takes precedence.
ADMIN_IP_WHITELIST=
ADMIN_IP_BLACKLIST=

# Encryption Configuration (AES-256-GCM)
# Generate a 32-byte base64 key: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY_BASE64=YOUR_32_BYTE_BASE64_ENCRYPTION_KEY_HERE
# Or use multiple keys for rotation (JSON array, newest first):
# ENCRYPTION_KEYS=["new_key_base64","old_key_base64"]

# Monitoring Configuration
LOG_LEVEL=debug
LOG_FORMAT=simple
LOG_SLOW_REQUEST_THRESHOLD=500
LOG_SKIP_PATHS=/health
LOG_MAX_FILES=7d
LOG_MAX_SIZE=10m
METRICS_ENABLED=true
TRACING_ENABLED=true

# Response Time Tracking
RESPONSE_TIME_ENABLED=true
RESPONSE_TIME_SLOW_THRESHOLD_MS=500
RESPONSE_TIME_WINDOW_SECONDS=60
RESPONSE_TIME_BUFFER_SIZE=1000

# Error Notification & Escalation
ERROR_NOTIFICATION_ENABLED=false
ALERT_ONCALL_EMAIL=oncall@chioma.app
ALERT_ESCALATION_EMAIL=platform-leads@chioma.app
ALERT_MANAGEMENT_EMAIL=
SLACK_ALERT_WEBHOOK_URL=
ALERT_ESCALATION_MINUTES=15
ALERT_ESCALATION_TIER2_MINUTES=30
ALERT_WEBHOOK_SECRET=change-me-alert-webhook-secret

# Dead Letter Queue (failed Bull jobs)
DEAD_LETTER_QUEUE_ENABLED=true
DEAD_LETTER_RETENTION_DAYS=30

# Database Connection Monitoring
DB_MONITORING_ENABLED=true
DB_POOL_WARNING_PERCENT=70
DB_POOL_CRITICAL_PERCENT=90
DB_QUERY_TIME_WARNING_MS=500
DB_QUERY_TIME_CRITICAL_MS=2000

# Database Index Review
DB_INDEX_UNUSED_MIN_SCANS=10
DB_INDEX_UNUSED_MIN_SIZE_MB=10

# API Versioning
API_LATEST_VERSION=v1
API_DEPRECATION_ENABLED=true
OPENAPI_GENERATE=false

# Database Pool & Replication
DB_TYPE=postgres
DB_SSL_REJECT_UNAUTHORIZED=true
TYPEORM_LOGGING=false
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_POOL_CONNECTION_TIMEOUT=5000
DB_POOL_IDLE_TIMEOUT=10000
DB_REPLICA_HOST=
DB_REPLICA_PORT=5432
DB_REPLICA_USERNAME=
DB_REPLICA_PASSWORD=
DB_REPLICA_NAME=
DB_REPLICATION_MAX_LAG_SECONDS=10
DB_ENCRYPTION_KEY=
DB_ENCRYPTION_KEY_VERSION=1
DB_ENCRYPTION_ROTATION_DAYS=90

# Auth Rate Limiting & MFA
MFA_REQUIRED=false

# Stellar Extras
HORIZON_URL=https://horizon-testnet.stellar.org
STELLAR_BASE_FEE=100
STELLAR_FRIENDBOT_URL=https://friendbot.stellar.org
STELLAR_ANCHOR_SECRET_KEY=
STELLAR_ENCRYPTION_KEY=
PROTOCOL_WALLET_ADDRESS=
PAYMENT_PROCESSING_CONTRACT_ID=
AGENT_REGISTRY_CONTRACT_ID=

# Storage / CDN
CDN_BASE_URL=

# Payment Webhooks
PAYMENT_WEBHOOK_SECRET=change-me-payment-webhook-secret

# Security Extras
SECURITY_HSTS_MAX_AGE=31536000
SECURITY_CSP_ENABLED=false
SECURITY_CONTACT=security@chioma.app
SECURITY_POLICY_URL=https://chioma.app/security
SECURITY_ACKNOWLEDGMENTS_URL=https://chioma.app/security/acknowledgments
SECURITY_PREFERRED_LANGUAGES=en
SECURITY_CANONICAL_URL=https://chioma.app/.well-known/security.txt
SECURITY_EXPIRES=
SECURITY_ENCRYPTION_KEYS=

# Observability (OpenTelemetry)
OTEL_ENABLED=true
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=
OTEL_LOG_LEVEL=info
LOG_ERROR_MAX_FILES=14d
LOG_DIR=logs
LOG_REDACT_KEYS=password,token,secret,authorization
LOG_REDACT_MAX_DEPTH=5
LOG_REDACT_PLACEHOLDER=[REDACTED]

# Webhooks
WEBHOOK_SIGNATURE_SECRET=change-me-webhook-signature-secret

# Request Size Limits
REQUEST_SIZE_LIMIT_JSON=1mb
REQUEST_SIZE_LIMIT_MULTIPART=10mb
REQUEST_SIZE_LIMIT_URLENCODED=1mb

# Graceful Shutdown
GRACEFUL_SHUTDOWN_TIMEOUT=10000

# Cache Warming
ENABLE_CACHE_WARMING=false

# Screening Providers (SmartMove / Experian)
USER_SCREENING_DEFAULT_PROVIDER=transunion_smartmove
USER_SCREENING_SANDBOX_MODE=true
FRAUD_HOOKS_ENABLED=false
TENANT_SCREENING_CONSENT_TTL_DAYS=90
TENANT_SCREENING_REPORT_TTL_DAYS=30
TRANSUNION_SMARTMOVE_API_URL=
TRANSUNION_SMARTMOVE_API_KEY=
EXPERIAN_CONNECT_API_URL=
EXPERIAN_CONNECT_API_KEY=

# OAuth2 (SSO)
OAUTH2_CLIENT_ID=
OAUTH2_CLIENT_SECRET=
OAUTH2_PROVIDER_URL=
OAUTH2_REDIRECT_URI=

# Certificate pinning for outbound HTTPS calls (Stellar Horizon, anchor API,
# payment gateways, OAuth2 providers, screening provider, webhooks).
# Disabled by default; enable once real pins are collected for each host
# (e.g. via `openssl s_client -connect host:443 | openssl x509 -noout -fingerprint -sha256`).
CERTIFICATE_PINNING_ENABLED=false
# JSON map of hostname -> array of allowed SHA-256 certificate fingerprints, e.g.
# CERTIFICATE_PINS={"api.paystack.co":["AA:BB:CC:...:FF"]}
CERTIFICATE_PINS=

# Runtime OpenAPI response schema validation (ResponseSchemaValidationInterceptor).
# Default (false) only logs mismatches between a handler's actual response and
# its declared @ValidateResponseSchema DTO. Set true once a route is verified
# clean to reject non-conforming responses instead.
RESPONSE_SCHEMA_VALIDATION_STRICT=false

# Orphaned database records cleanup (OrphanedRecordsCleanupService, runs daily).
# Default (false) only logs/report counts of orphaned rows found. Set true to
# actually delete them once you've reviewed the reported counts.
ORPHAN_CLEANUP_DELETE_ENABLED=false

# OpenAI (property wizard assist)
OPENAI_API_KEY=

# Alerting / Escalation
ALERT_ONCALL_EMAIL=oncall@chioma.app
ALERT_ESCALATION_EMAIL=platform-leads@chioma.app
ALERT_MANAGEMENT_EMAIL=
SLACK_ALERT_WEBHOOK_URL=
ALERT_ESCALATION_MINUTES=15
ALERT_ESCALATION_TIER2_MINUTES=30
ALERT_WEBHOOK_SECRET=change-me-alert-webhook-secret
ERROR_NOTIFICATION_ENABLED=false

# Response Time Tracking
RESPONSE_TIME_ENABLED=true
RESPONSE_TIME_SLOW_THRESHOLD_MS=500
RESPONSE_TIME_WINDOW_SECONDS=60
RESPONSE_TIME_BUFFER_SIZE=1000

# Dead Letter Queue
DEAD_LETTER_QUEUE_ENABLED=true
DEAD_LETTER_RETENTION_DAYS=30

# Database Connection Monitoring
DB_MONITORING_ENABLED=true
DB_POOL_WARNING_PERCENT=70
DB_POOL_CRITICAL_PERCENT=90
DB_QUERY_TIME_WARNING_MS=500
DB_QUERY_TIME_CRITICAL_MS=2000

# Database Index Review
DB_INDEX_UNUSED_MIN_SCANS=10
DB_INDEX_UNUSED_MIN_SIZE_MB=10

# Agent / User / Tenant / Landlord Seed Configuration
AGENT_DEFAULT_EMAIL=agent@chioma.local
AGENT_DEFAULT_FIRST_NAME=Demo
AGENT_DEFAULT_LAST_NAME=Agent
AGENT_AUTO_GENERATE_PASSWORD=true
USER_DEFAULT_EMAIL=user@chioma.local
USER_DEFAULT_FIRST_NAME=Demo
USER_DEFAULT_LAST_NAME=User
USER_AUTO_GENERATE_PASSWORD=true
TENANT_DEFAULT_EMAIL=tenant@chioma.local
TENANT_DEFAULT_FIRST_NAME=Demo
TENANT_DEFAULT_LAST_NAME=Tenant
TENANT_AUTO_GENERATE_PASSWORD=true
LANDLORD_DEFAULT_EMAIL=landlord@chioma.local
LANDLORD_DEFAULT_FIRST_NAME=Demo
LANDLORD_DEFAULT_LAST_NAME=Landlord
LANDLORD_AUTO_GENERATE_PASSWORD=true
